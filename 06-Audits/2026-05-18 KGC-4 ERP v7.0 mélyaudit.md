---
name: KGC-4 ERP v7.0 mélyaudit (BMAD Phase 5)
type: audit
created: 2026-05-18
updated: 2026-05-18
tags: [audit, kgc-erp, "#project/kgc-erp"]
related: [kgc-berles, robbantott-kereso]
session: 2026-05-18-kgc-all
---

# KGC-4 ERP v7.0 — mélyaudit (2026-05-18)

> Audit-elt verzió: shallow-clone `/tmp/kgc4-audit-2026-05-18/` (depth=100). Repo: [zolijavos/KGC-4](https://github.com/zolijavos/KGC-4) (PRIVATE). Last commit: `fde583b` 2026-05-18 09:30 (`fix(payroll): Sprint 26 W1 D-3 — Q-T012 P0 PayslipDispatch queue-isolation`). Sprint 26 W1 D-3 fut.
>
> Az audit kontextusa: a [[02-Projects/kgc-berles]] (Next.js bérlés-frontend, `:3004`) integrációja a Zoli-féle ERP-vel. Eddig a vault `KGC-3`-ra mutatott (eltűnt prod path + parkolt dev) — ez tévedés. **Az aktív KGC-ERP fejlesztés a KGC-4-ben van.**

## 1. Repo-állapot összegzés

- **Branch:** `main` (single-trunk workflow, feature-branchek lokálisan)
- **Last commit:** `fde583b` — 2026-05-18 09:30 (`fix(payroll): Sprint 26 W1 D-3 — Q-T012 P0 PayslipDispatch queue-isolation`)
- **Sprint-état:** **Sprint 26 W1 D-3** fut. Sprint 26 W0 lezárva 2026-05-18 (Foundation Closure 100% + Mary mini-discovery prep). Phase 1 ~90-95% kész, hátralévő ~3-10 SP a "Mary discovery" kliens-callra vár.
- **Fejlesztő-aktivitás (30 nap):** 100+ commit. Az utolsó 3 nap density: **2026-05-16 = 55 commit, 2026-05-17 = 34 commit, 2026-05-18 = 11 commit** — Zoli intenzív solo-cluster-burst-tel dolgozik, sprint-day-onként 5-15 commit jellemző.
- **Verzió-jelölés:** `package.json version: 3.0.0`, README "KGC ERP v7.0", code-ban `kgc-3` prefix is — branding-tisztázás nem teljes (lásd §10).
- **Local clone:** [/root/projektjeim/KGC-ALL/KGC-4](file:///root/projektjeim/KGC-ALL/KGC-4) — HEAD `d434a3c` 2026-03-25, **1337 commit behind origin/main**, 13 modified + 27 untracked drift. Audit ezt NEM érintette (fresh clone /tmp-be).

## 2. Tech-stack

| Réteg | Konkrét tech | Verzió |
|---|---|---|
| **Backend** | NestJS + TypeScript strict | NestJS **10.x**, TS 5.3+ |
| **Frontend (admin)** | React + Vite + shadcn/ui + Zustand + TanStack Query | React **19.2**, Vite 5, Tailwind **4.1** |
| **DB + ORM** | PostgreSQL + Prisma (`prismaSchemaFolder` preview) | PG **16**, Prisma 5.x |
| **Cache / Queue** | Redis + BullMQ (queue: `email`, `payslip-email`) | Redis 7 |
| **Auth** | JWT (admin) + külön **Portal JWT** (OTP/Magic-link, jelszó-mentes) | + Keycloak strategy stub (`packages/core/auth/src/strategies/keycloak-jwt.strategy.ts`) |
| **Test stack** | Vitest 2.1 (unit + integration), Playwright 1.48 (E2E + security/RLS-fuzz), Stryker (csak `@kgc/tenant` mutation, `test:mutation:tenant`) | + `@testcontainers/postgresql` |
| **CI/CD** | GitHub Actions: 12 workflow (`ci`, `test`, `build-images`, `deploy`, `burn-in`, `security-tests`, `ai-review`, `convention-lock`, `nightly-video`, `monthly-export-smoke`, `deployment-test`, `build-twa`) | |
| **Monorepo** | pnpm 9.15 workspaces + Turborepo 2.3 | Node 20+ |
| **Egyéb** | Helmet + CSP, cookie-parser, Sentry (`instrument.ts`), MinIO (storage), Caddy/Nginx, Husky + lint-staged, hCaptcha (public-inquiry) | |

## 3. Monorepo szerkezet

**Apps (7):**

| App | Funkció |
|---|---|
| `apps/kgc-api` | NestJS backend — **68 modul, ~153 controller** (`*.controller.ts` spec-mentes); REST `/api/v1/*` prefix |
| `apps/kgc-web` | React 19 + Vite PWA admin/operátori frontend (offline-first, service-worker) |
| `apps/kgc-dashboard` | React dashboard (7 nézet, KPI + sprint metrika) |
| `apps/kgc-portal` | **Customer portal** (Vite + React, OTP/Magic-link auth, ez a "B2C" portál) |
| `apps/kgc-admin` | Admin panel (váz) |
| `apps/kgc-android-twa` | Android Trusted Web Activity wrapper |
| `apps/kgc-ux-showcase` + `rpi-skool-video-gen` | UI komponens-katalógus + segéd-projekt |

**Packages (7 top-level domain, ~43-58 alpkg):**

| Top-level | Tartalom |
|---|---|
| `packages/core` | `auth`, `tenant` (RLS service + interceptor), `audit`, `config`, `common`, `users`, `chatwoot-bridge` |
| `packages/shared` | `ui`, `i18n`, `partners`, `products`, `accounting`, `product-catalog` (saját Prisma schemával), `testing`, `inventory`, `workflow` (rPI Flow) |
| `packages/rental` (README "berles") | `rental-core`, `rental-checkout`, `rental-contract`, `rental-equipment` |
| `packages/service` (README "szerviz") | `service-core`, `service-worksheet`, `service-warranty`, `service-standards`, `service-hr`, `service-payroll` |
| `packages/store` | `sales-pos`, `sales-invoice`, `sales-quote`, `goods-receipt`, `pricing-margin`, `product-catalog` |
| `packages/integration` | `nav-online`, `invoicing-engine`, `messaging-gateway`, `twenty-crm`, `chatwoot`, `horilla-hr`, `koko-ai`, `online-booking`, `tesseract-provider`, `equipment-service` |
| `packages/gyartas` | Gyártás-domain (Manufacturing modul) |

Konvenció: a **packages/** alatti mapparéteg egy domain-csomag-fa, az `apps/kgc-api` modulokon keresztül "wire-eli be" őket (`AppModule.imports`).

## 4. Adatmodell — kulcsmodellek

A Prisma schema **`apps/kgc-api/prisma/schema/`** alá darabolva (23 .prisma fájl, ~370 KB összesen, `prismaSchemaFolder` preview-feature). A README "72 modell"-t említ. A **kgc-berles felé legrelevánsabb 12 model**:

| Model | Domain | Kulcs-mezők / megjegyzés |
|---|---|---|
| **Tenant** (`tenants`) | Multi-tenancy | `id`, `slug`, `parentTenantId` (holding-hierarchia), `status`, `schemaName` |
| **PortalUser** (`portal_users`) | **Portál ügyfél** (E-73) | jelszó-MENTES; `phone` + `email` + `name`; SMS OTP / Magic-link; `notificationPrefs` JSON; **`partnerId` → Partner** kapcsolat (azaz a "portál-user" reálisan egy Partner-re mappel) |
| **PortalSession** | Portál refresh tokenek | `tokenHash` SHA-256, `expiresAt` |
| **Partner** (`partners`) | Ügyfél törzs | `partnerCode`, `type` (INDIVIDUAL/COMPANY), adószám, **kategorizált kedvezmény** (`discountRental` / `discountSales` / `discountService` max 15%), `creditLimit`, `loyaltyPoints`, blacklist, e-invoice, költségviselő-self-rel |
| **Booking** (`bookings`) | **Online foglalás** (E-73-S9) | `bookingCode`, `status` enum: PENDING / CONFIRMED / REJECTED / CANCELLED / **CONVERTED** (=> rentallé alakult); `startDate` / `endDate`; `partnerId`, `locationId`, `rejectReason`, `rentalId` (a konverziós cél) |
| **BookingItem** | Foglalás-sor | `equipmentId` → RentalEquipment, `quantity`, **árazás-snapshot** (`dailyRate`) |
| **RentalEquipment** (`rental_equipment`) | **Bérgép (eszköz-szintű)** | `equipmentCode`, `serialNumber`, `productId`, `status` (8 lifecycle: AVAILABLE/RENTED/RESERVED/IN_SERVICE/DAMAGED/LOST/SOLD/SCRAPPED), **dailyRate / weeklyRate / monthlyRate / hourlyRate / depositAmount / depositPercent**, totalRentals/totalRevenue stats, QR-kód, gyári-barcode |
| **EquipmentType** (`equipment_types`, E-123 új) | **Bérgép típus-katalógus** | `name`, `category`, `sku` (KGC cikkszám), `manufacturerSku`, `alternativeSkus[]`, `brand`, `model`, `description`, **`heroImageUrl` + `galleryImages[]`** (webshop face), `specsJson` |
| **EquipmentUnit** (`equipment_units`, E-123 új) | **Fizikai szériaszámú példány** | `typeId` → EquipmentType, `serialNumber`, `assetTag`, `status` (8-érték), `operatingHours` |
| **Rental** (`rentals`) | Bérlés-tranzakció | `rentalCode`, `partnerId`, `status` (DRAFT/CONFIRMED/ACTIVE/OVERDUE/RETURNED/COMPLETED/CANCELLED), `subtotal/discount/lateFee/totalAmount/vatAmount/grandTotal`, **`depositRequired/Paid/Returned/Retained`** (4-állású kaució-tracking) |
| **RentalItem** | Bérlés-sor | `equipmentId`, `appliedRate`, `totalDays`, `itemTotal` |
| **Product** + **ProductCategory** | Termék-katalógus (Epic 8) | `ProductType` enum: PRODUCT/RENTAL_EQUIPMENT/PART/CONSUMABLE/SERVICE; **11-értékes `ProductGroupCode`** (GEP, BERGEP, ALKATRESZ, TARTOZEK…); hierarchikus kategória + materializált `path` |
| **PortalReview** (`portal_reviews`) | Lezárás-utáni 1-5 csillagos értékelés | `referenceType` RENTAL/SERVICE, `rating`, `comment` |

**Domain-tagolás (5 fő doménblokk a schema-folder szerint):**
- **Core/Tenant/User** (`core.prisma` 42KB) — Tenant, User (RBAC 8-szintű Role), Location, Conversation, BusinessHours, Workflow, EmailTemplate
- **Inventory + Equipment + Product** (`inventory.prisma` 33KB + `product.prisma` 32KB + `equipment.prisma` 5KB)
- **Rental + Reservation** (`rental.prisma` 28KB + `reservation.prisma`)
- **Service + Worksheet** (`service.prisma` 25KB)
- **Finance + Sales + Invoice + Payroll** (`finance.prisma` 15KB + `sales.prisma` 21KB + `payroll.prisma` 6KB)
- **Portal** (`portal.prisma` 6KB)
- **Partner** (`partner.prisma` 22KB) + **Integration** (`integration.prisma` 22KB, NAV-Online, MyPOS)
- **HR + Documents + Notification + ESign + AuthGrant + Porter + Procurement + TenantSecret** (kisebbek)

## 5. API-modulok térképe

A `kgc-api` 68 modulja közül a frontend-integrációs szempontból a portal-csomag a kulcs.

| Modul | Cél | Auth | Publikus a Next.js-nek? |
|---|---|---|---|
| **portal-equipment** | Bérgép katalógus + naptár (E-73) | **NINCS guard** (`/** Public controller */`) | **IGEN** — list / detail / availability |
| **portal-locations** | Telephelyek listája | (a portal-equipment modulban) | IGEN |
| **portal-auth** | OTP + Magic-link + register + refresh (zod-validáció) | publikus | IGEN |
| **portal-bookings** | Foglalás CREATE/LIST/CANCEL | `@UseGuards(PortalAuthGuard)` | IGEN (auth után) |
| **portal-rentals** | Saját bérlések + extension-request | PortalAuthGuard | IGEN |
| **portal-services** | Szerviz munkalapok | PortalAuthGuard | IGEN |
| **portal-invoices** | Számla-lista + PDF + fizetés | PortalAuthGuard | IGEN (+ payment-callback publikus) |
| **portal-payments** | Fizetési flow | publikus callback | (callback) |
| **portal-profile** | Profil + GDPR-delete + notification-prefs | PortalAuthGuard | IGEN |
| **portal-reviews** | 1-5 csillagos értékelés | PortalAuthGuard | IGEN |
| **portal-notifications** | Push/SMS prefs + read | PortalAuthGuard | IGEN |
| **portal-dashboard** | Aggregált home (bérlések+szervizek+invoice) | PortalAuthGuard | IGEN |
| **portal-extensions** | Bérlés-hosszabbítás kérelmek | PortalAuthGuard | IGEN |
| **online-booking** | "Régi" online-booking domain (in-memory repo!) — duplikál a portal-bookings-szal, E-26 epic | DI, valószínűleg admin-side | **DEPRECATED?** (in-memory, nem prod) |
| **public-inquiry** | `POST /public/inquiry` — kapcsolat-űrlap | publikus, **hCaptcha + 5 req/min/IP** + honeypot | IGEN |
| **public-worksheet** (service modul) | `GET /public/worksheets/:token/status` | publikus, 64-hex token | IGEN (csak QR-flow) |
| **public-service-request** (service modul) | `POST /public/service-request` (pre-reg) | publikus, 3 req/h/IP | IGEN (pre-reg flow) |
| auth (admin) | JWT login, refresh, PIN, pwd-reset | publikus → JWT | (nem portal) |
| ~50 üzleti modul (accounting, inventory, hr, payroll, pos, procurement, vehicles, manufacturing, stb.) | admin/operátori | JWT + role | NEM |

**Globális prefix:** `/api/v1` (kivéve `/`).
**Tenant resolution:** publikus endpointokon **`Origin` header alapú** (lásd `public-inquiry.service`). Portal-flow-knál a `tenantId` JWT payload-ból. Az unauthenticated `portal-equipment` viszont **query-paraméterben** veszi a `tenantId`-t (`?tenantId=...`) — ezt érdemes a frontend integrációnál konfig-konstansból tölteni, NEM userspace-ből.

## 6. Auth + RBAC

- **Két párhuzamos auth-rendszer:**
  1. **Admin-auth (`packages/core/auth`)** — klasszikus email+jelszó JWT, refresh-token cookie (`httpOnly`), `Role` enum 8 szint: `OPERATOR(1) / TECHNIKUS(2) / BOLTVEZETO(3) / ACCOUNTANT(3) / PARTNER_OWNER(4) / CENTRAL_ADMIN(5) / DEVOPS_ADMIN(6) / SUPER_ADMIN(8)`. PIN-login is van pultos-flowhoz. Endpoint-ok: `/auth/login`, `/refresh`, `/logout`, `/logout-all`, `/pin-login`, `/forgot-password`, `/reset-password`, `/verify-password`.
  2. **Portal-auth (`apps/kgc-api/src/modules/portal-auth`)** — **jelszó NÉLKÜLI**, SMS OTP + Magic-link email. Külön `PortalUser` tábla, külön `PortalAuthGuard`, `req.portalUser.{id,tenantId,partnerId}` szemantika.
- **Multi-tenancy:** PostgreSQL **Row Level Security (RLS)** policies (`prisma/policies/rls-policies.sql`). `RlsService` + `TenantContextInterceptor` automatikusan injektálja a `tenant_id`-t Prisma middleware-en keresztül — a `findMany({where:{tenantId}})` manuális szűrés **TILTOTT** (AGENTS.md "Critical Rule #1"). Schema-szinten **minden tábla `tenantId: Uuid`** és RLS-policy van rá.
- **6 RLS-batch migráció** Sprint 24 W3 körül landed: `batch1_rls_auth → batch6_rls_integration` + 2026-05-15 `e107_rls_policies_consolidation`. A `BYPASS_RLS` mód `BypassRlsPrismaService`-en keresztül opt-in (admin tooling).
- **API rate-limit:** `ApiRateLimitGuard` (Redis-alapú, in-memory fallback) globálisan; publikus endpoint-ok külön throttle (5/min, 3/h).
- **Külső API auth (kgc-berles → kgc-api):** nincs külön API-key infrastruktúra. **Két reális opció:**
  1. `portal-equipment` style **anonymous + tenantId-query** (ami most már megvan és működik) — a Next.js BFF/API-route ezt simán kihasználhatja.
  2. Új service-account JWT vagy `TenantSecret` (`packages/core/tenant/src/prisma-extensions` + `tenant-secret.prisma` table) — ADR-112 már encrypted per-tenant API-keys-t használ provider-integrációkhoz, ezt lehet bővíteni.

## 7. BMAD Phase 5 = mi

A **BMAD = "Business-driven Multi-AI Development"** — Zoli saját agentic workflow-rendszere, ami a `_bmad/` mappában tárol konfigot (`bmm`, `cis`, `tea`, `wds`, `bmb` sub-module-ok, `config.toml`, scripts), és a `_bmad-output/`-ba teszi az artifact-eket (sprint-retro-k, ATDD checklist-ek, brainstorming, AI cross-review-k).

**Fázisok eddig:**
- Phase 1-4 = a tényleges feature-build (Sprint 1-25, ~80 epic, ~470 story implemented).
- **Phase 5 = "Tesztelési Fázis"** = stabilizáció + foundation-hardening:
  - Test-coverage emelés (vitest unit + Playwright E2E + security-fuzz)
  - **RLS-policy consolidation** (E-107)
  - **CREATE TYPE idempotency wrap** (Phase 6 + 6.1 + 6.2 — 136 CREATE TYPE statement idempotensé tétel)
  - **Q-T quality tracker tasks** (Q-T008..Q-T014) = adversarial review-ban talált P0/P1 hibák (pl. Q-T012 = PayslipDispatch silent-failure-loop a production-ben — `BullMQ.email` queue szennyezés miatt soha nem ment el bérlapfizetési email, fix queue-isolation-nel)
  - **"Murat" middle-way pathway** = adversarial review-ban talált hibákra batch-stratégia (Murat strategy A/B/C/D — smoke-healing, baseline-fix, cross-module scaffold, type-safety)
  - **"Foundation closure"** = a foundation-layer (DB migration + RLS + auth) blokkoló-mentesítése a következő nagy feature-sprint előtt
- **Sprint 26 W0 (most lezárva, 2026-05-18):** 7.9 SP, 6 commit, Phase 6/6.1/6.2 complete + Mary-prep ready.
- **Sprint 26 W1 (D-3-ban most):** Q-T008→Q-T012 tracker-cleanup, Murat A+B+C+D batchek, payslip-fix, e2e PCJ-11 cross-tenant status-code fix. Várja a **"Mary mini-discovery"-t**.
- **"Mary discovery" =** a `Mary` egy **BMAD persona** (analyst/discovery-role, vélhetően a BMAD bmm-modul agent-je) — a "Szerviz PM #2 scope-discovery kliens-mini-egyeztetés" mini-discovery prep szerint **4 kérdéses kliens-egyeztetés Javo/Zsuzsi-val a Szerviz-modul gap-finding-jére**.
- **Várható Sprint 26 W2+:** Mary-call-driven Service slice batch (~3-10 SP).

## 8. Integration-readiness a kgc-berles felé

**Ami MÁR megvan és azonnal használható:**

| Funkció | Endpoint | Auth | Megjegyzés |
|---|---|---|---|
| **Bérgép-katalógus list** | `GET /api/v1/portal/equipment?tenantId=&category=&search=&locationId=&page=&pageSize=` | NINCS | Visszaad: `{data:[{id, name, category, brand, dailyRate, weeklyRate, availability:'available'/'rented'/'in_service'}], total, page, pageSize}` — pageSize max 50, ár Math.round-olt integer Ft |
| **Bérgép-detail** | `GET /api/v1/portal/equipment/:id?tenantId=` | NINCS | dailyRate/weeklyRate/monthlyRate/depositAmount, condition, model, leírás |
| **Naptár-availability** | `GET /api/v1/portal/equipment/:id/availability?tenantId=&startDate=&days=` | NINCS | 30-napos default range, busy-set az ACTIVE+CONFIRMED rental-itemek alapján |
| **Telephely-lista** | `GET /api/v1/portal/locations` | NINCS | telephelyek per tenant |
| **OTP signup/login** | `POST /portal/auth/request-otp` → `verify-otp` | publikus | jelszó-mentes flow |
| **Magic-link** | `POST /portal/auth/request-magic-link` → `GET verify-magic-link?token=&tenantId=` | publikus | email-alapú |
| **Foglalás létrehozás** | `POST /portal/bookings` | Portal JWT | `BookingItem[]` (equipmentId + quantity), `dailyRate` snapshot az item-ben |
| **Foglalás-lista / cancel** | `GET /portal/bookings`, `DELETE /portal/bookings/:id` | Portal JWT | PENDING / CONFIRMED / REJECTED / CANCELLED / CONVERTED |
| **Bérlés-lista + extension** | `GET /portal/rentals`, `POST /portal/rentals/:id/extend-request` | Portal JWT | aktív + historikus |
| **Számla + PDF + fizetés** | `GET /portal/invoices/:id/pdf`, `POST /portal/invoices/:id/pay` | Portal JWT | MyPOS integráció (stub-szinten) |
| **Public inquiry** | `POST /public/inquiry` | publikus + hCaptcha + 5/min/IP | kapcsolat-űrlap landing-pagehez |
| **Public service request** | `POST /public/service-request` | publikus + 3/h/IP | szerviz pre-regisztráció |

**Ami HIÁNYZIK (a kgc-berles épülésekor érintheted Zolival):**

1. **Cikk / CMS / blog modul: NINCS.** Sem `Article`, `Post`, `Content`, `Page` model nem létezik. Ha a Next.js site-on cikkeket akarsz, az **NEM jön a KGC-4-ből** — vagy külön headless CMS (Sanity/Strapi/Payload), vagy a Next.js-ben fájlrendszer-alapú MDX kell.
2. **`EquipmentType` még nem szerepel a portal-equipment API-ban.** A portal-equipment a "régi" `RentalEquipment`-en operál, NEM az új `EquipmentType` katalóguson (E-123 új tábla, `heroImageUrl` + `galleryImages[]` mezőkkel). **Ezt kérni kéne**, mert a webshop-face képek és gallery oda kerülnek.
3. **Termék-image / Equipment-image: a portal-equipment most NEM ad vissza képet.** A schema-ban van `EquipmentType.heroImageUrl` és `Product.images` (feltehetően), de a `PortalEquipmentListItem` interface csak: `id, name, category, brand, dailyRate, weeklyRate, availability`. **Bővítendő API.**
4. **SSO a két frontend között (admin ↔ public site):** nincs cross-app cookie kontraktus most. Ha "MyKGC" portal-userrel SSO-t akarsz a kgc-berles felé, vagy a portal-JWT-t kell shared-secret-szel verify-elni Next-en, vagy NextAuth-szal teljesen külön session.
5. **Real-time / WebSocket:** `apps/kgc-api/src/modules/dashboard/websocket/` van (admin-side dashboard events), de **portal-side notification push NINCS** — a `notification` és `portal-notifications` modulok REST-pollos.
6. **Public catalog feed (CSV/JSON sitemap):** nincs export-endpoint a webshop-szinkronhoz. Ha SEO-szinten egy `/products.json` feed kell, kérni kell.
7. **Versioning:** `/api/v1` van, de a portal-route-okon NINCS verzió-prefix (Swagger doc szerint `/portal/auth/...` — valójában a global-prefix miatt `/api/v1/portal/...`). Tisztázandó.

**Javaslat a kgc-berles architektúrára:**
- **Next.js (`:3004`) → BFF route-okon → KGC-4 API (`/api/v1/portal/*` és `/api/v1/public/*`)**.
- A `tenantId` legyen **env-konstans** a Next-app `.env`-ben (`KGC_TENANT_ID=...`), NEM URL-paraméter.
- Authentikációhoz: indulj **anonymous** flow-val (csak `portal-equipment` + `public-inquiry` + `public-service-request`), és **opt-in** OTP/Magic-link → `/portal/bookings` POST a foglalás-flowhoz.
- Image-eket: amíg `portal-equipment` nem adja vissza a `heroImageUrl`-t, addig **közvetlenül a Product/EquipmentType endpoint kell** (vagy bővítsd a `PortalEquipmentService.list/detail` `select`-et — apró PR Zoli-nak).
- A [[11-wiki/nextjs-api-proxy-bridge]] wiki-mintával csináld — egyezik a balance/example-balance.local stílusoddal.

## 9. Legutóbbi sprint-ek tartalmi összefoglaló

| Sprint | Tartalom |
|---|---|
| **Sprint 24** (closed) | **E-126 Procurement Phase 1 FULL CLOSURE** (4/4 source-paths + Boltvezető cart UI), i18n placeholder konvenció-egyesítés |
| **Sprint 25 W0** | S-126-08 cart-submit + mark-group-ordered + supplier-routing |
| **Sprint 25 W1** | **ADR-129 Inventory cikkszám-minimal slice** (`Product.internalCode` auto-gen `KGC-{YYYY}-{seq:04d}` séma, `TenantProductSequence` table), `ProductCodeSequenceService` SQL hotfix, supplier-sku BadRequest fix |
| **Sprint 25 W2** | **Foundation-gate Murat middle-way ratify** — messaging-gateway `email.processor.ts` ALS-RLS adoption (G-001 FLAG resolve), Playwright + vitest smoke-coverage (Option B), Product spec schema-drift fix (`unit + createdBy + updatedBy` mezők); CI Full Test Suite zöld |
| **Sprint 26 W0** | **Foundation Closure 100%** — Phase 6+6.1+6.2 (136 CREATE TYPE statement idempotent wrap, új `wrap-create-type.py` tool), E-93 SCOPE-EMPTY, 3 YAK-shave megelőzve, Mary mini-discovery prep doc + Service module state-audit (14 ctrl + 28 spec EXTENSIVELY implemented), 4 NEW memory rule formalizálva |
| **Sprint 26 W1 D-1..D-3** (folyamatban) | Adversarial review + 5 surgical fix; **Q-T008 smoke-healing**, **Q-T009 audit**, **Q-T010 PCJ-11 cross-tenant status-code drift fix**, **Q-T012 P0 PayslipDispatch queue-isolation** (production silent-failure-loop fix: bérlap-emailek soha nem mentek el a PAY-9 BullMQ migráció óta, `email` queue → `payslip-email` queue izoláció, 6 unit test 6/6 PASS, **Q-T014 spawnolva** = NestPayslipProcessor implementation Sprint 26 W2 candidate), Murat A+B+C+D batch (smoke + baseline + cross-module scaffold + tracker + type-safety + S-CROSS-001), doc-audit fix G1+G2+G3 sync gaps closed. **Várakozó-state**: Mary kliens-call slot scheduling (Javo/Zsuzsi action). |

**"Q-T" = Quality-Tracker task** = adversarial review (BMAD party-mode + Murat strategy) által feltárt P0/P1 hibák, sprint-en kívül kezelt patch-ek. Sprint 26 W1 tervezett SP-cap mellett "Mary-call-waiting" alatt ad-hoc paced.

## 10. Tisztázandó pontok (Zoli-call-agenda kandidátok)

1. **Verzió-jelölés inkonzisztens:** package.json `3.0.0` + `kgc-erp` name; README "v7.0"; AGENTS.md "rPI ERP v7.0 (KGC-4)"; CLAUDE.md ugyanígy; description "Sprint 26 W1 = Phase 5". → **Kérdezz Zolit: mit jelent a v7.0 vs v3.0?**
2. **`online-booking` modul (in-memory repository) vs `portal-bookings` (Prisma-backed)** — duplikál? Az `online-booking.module.ts` `InMemoryBookingRepository`-t használ, ami nem prod. Kérdés: ez deprecated és csak `portal-bookings` az aktív? Vagy E-26 párhuzamos legacy?
3. **Termék-képek a portal-API-ban:** a `portal-equipment.service` `select` jelenleg NEM adja vissza a `Product.images` / `EquipmentType.heroImageUrl`-t — **Zoli-kérés**: bővítse a select-et, vagy adja meg az URL-konvenciót (MinIO publikus bucket? CDN-front?).
4. **`tenantId` átadás a publikus API-n** — most query-string. Production-ben kell-e Origin-alapú resolve (mint a `public-inquiry`-ben)? Mert ha kgc-berles `:3004` is más originról jön mint a kgc-portal, akkor a `CORS_ORIGINS` env-be is fel kell venni + tenant-resolution stratégiát egyértelműsíteni.
5. **API-versioning a portal-routes-on:** `/api/v1/portal/*` lesz a tényleges URL (global-prefix miatt) — confirmed.
6. **EquipmentType vs RentalEquipment** — két párhuzamos katalógus-modell. A "webshop-face" (E-123 STORY-1306 D5) **`EquipmentType.heroImageUrl`-be** kerül, de a `portal-equipment` még a régi `RentalEquipment`-tel dolgozik. Mi a roadmap a két modell egyesítésére / a portal-API E-123-as átállására?
7. **Cikk / CMS modul:** nincs. Megerősítendő, hogy ezt a kgc-berles oldalán külön kezeljük (MDX / Sanity / Payload). Ha integrálni akarjuk a KGC-4-be, az egy új epic-feature.
8. **MyPOS / payment-callback flow:** a kód szerint még stub-szinten. Production-ben a public site fizetés-redirect csak akkor működik, ha ez bekapcsolódik.
9. **`apps/kgc-portal`** — már létezik egy portal-frontend app. Mi a viszonya a tervezett `kgc-berles`-hez? Külön landing-page lesz csak (kgc-berles = marketing-site), vagy a portal-flow-t is meghívja?
10. **Rate-limit a portal-equipment endpointokon:** **NINCS `@Throttle` decorator!** Csak a globális `ApiRateLimitGuard` védi. A `public-inquiry` 5/min/IP-t kapott, a `public-service-request` 3/h-t — a `portal-equipment` list/detail nyitva van. SEO-bot vs adversarial bot ellen kérni kell hardenelést.

## 11. Konkrét fájl-pointerek

Relatív path-ek a repo-gyökértől (`/tmp/kgc4-audit-2026-05-18/`):

1. **`README.md`** — projekt-belépő (stack, integráció-státusz táblázat).
2. **`AGENTS.md`** + **`CLAUDE.md`** — agent-instrukciók (RLS-szabály, TS-strict, branch-flow, output-szabályok).
3. **`apps/kgc-api/prisma/schema/portal.prisma`** — `PortalUser`, `PortalSession`, `Booking`, `BookingItem`, `PortalReview`. **Itt nézz be elsőként** a kgc-berles foglalási flowhoz.
4. **`apps/kgc-api/prisma/schema/rental.prisma`** — `RentalEquipment`, `Rental`, `RentalItem`, `RentalExtension`, `Deposit`, `EquipmentProfitSnapshot`.
5. **`apps/kgc-api/prisma/schema/equipment.prisma`** — **E-123 új** `EquipmentType`, `EquipmentUnit`, `ServicePackage`. Itt vannak a webshop-face mezők (`heroImageUrl`, `galleryImages`).
6. **`apps/kgc-api/prisma/schema/core.prisma`** (Tenant + User), **`partner.prisma`** (Partner), **`product.prisma`** (Product + ProductCategory).
7. **`apps/kgc-api/src/modules/portal-equipment/portal-equipment.controller.ts`** + **`portal-equipment.service.ts`** — **a kgc-berles direkt fogyasztja**. 220 sornyi szolgáltatás, simán bővíthető a képek hozzáadásával.
8. **`apps/kgc-api/src/modules/portal-auth/portal-auth.controller.ts`** + **`portal-auth.service.ts`** — OTP/Magic-link flow, Zod schemák a `dto/` alatt.
9. **`apps/kgc-api/src/modules/portal-bookings/portal-bookings.controller.ts`** — foglalás-CRUD (CREATE/LIST/CANCEL).
10. **`apps/kgc-api/src/modules/public-inquiry/public-inquiry.controller.ts`** — minta a publikus, anonymous-throttled flow-ra (hCaptcha + Origin-tenant-resolve). Mintaként használd ha kgc-berles oldalra is kapcsolat-formot teszel.
11. **`apps/kgc-api/src/main.ts`** — bootstrap: `/api/v1` prefix, CORS `CORS_ORIGINS` env-list, helmet+CSP, Swagger `/api/docs` (csak NEM-prod).
12. **`apps/kgc-api/src/app.module.ts`** — 68 modul wire-up; itt látod hogy mi van importálva.
13. **`docs/api-contracts-kgc-api.md`** — **A 636 endpoint teljes leírása**, §51 a Portal-blokk.
14. **`docs/data-models-kgc-api.md`** — Prisma model katalógus (72 model + 50 enum, 17 domain-csoport).
15. **`_bmad-output/reports/sprint-26-w0-close-retro-2026-05-18.md`** — Foundation Closure retro (a legfrissebb projekt-état overview).
16. **`_bmad-output/reports/mary-mini-discovery-szerviz-pm-2-prep-2026-05-18.md`** — a "Mary discovery" prep doc, a Service-modul ground-truth state-audit + 4 kliens-kérdés.
17. **`docker-compose.yml`** + **`infra/docker/`** — futtatási környezet (PG16 + Redis7 + MinIO + kgc-api + kgc-web).
18. **`apps/kgc-api/prisma/policies/rls-policies.sql`** — RLS-policy SQL (multi-tenancy alap).

## Kapcsolódó

- [[02-Projects/kgc-erp]] — projekt-fájl (KGC-4-re átírva)
- [[02-Projects/kgc-berles]] — a Next.js bérlés-frontend ami integrálni fog
- [[07-Decisions/2026-05-18 KGC-ERP kanonikus repo = zolijavos KGC-4]] — repo-switch ADR
- [[11-wiki/nextjs-api-proxy-bridge]] — Next.js → REST-backend pattern
- [[08-Sessions/2026-05-18-kgc-all]] — audit-session
