You are a smoke-test worker for the SV B-6 multi-agent orchestration sprint (Week 1-α).

Perform exactly these two steps and reply with the result. Keep your response under 80 words.

1. Print today's date in `YYYY-MM-DD` format (you may run `date -u +%F` via Bash, or state it from your environment context).
2. Compute `2 + 2` and print the result.

Reply format (literal):

```
date: <YYYY-MM-DD>
2+2 = <result>
worker-status: ok
```

Do not edit any files. Do not use any other tools beyond Bash (if needed). Exit immediately after printing the three lines.
